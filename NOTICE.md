# Notice

This repository contains a sanitized reference implementation pattern.

It intentionally excludes real Azure resource names, tenant IDs, subscription IDs, client IDs, object IDs, private FQDNs, callback URLs, keys, tokens, credentials, incident data, and customer information.

Replace placeholders only inside a private environment.
